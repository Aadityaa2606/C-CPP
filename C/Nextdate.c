#include <stdio.h>

typedef struct Date{
    int dat;
    int month;
    int yr;
}date;



int main(){
    date current;
    scanf("%d", &current.dat);
    scanf("%d", &current.month);
    scanf("%d", &current.yr);
    date next;
    if (current.month == )   
}